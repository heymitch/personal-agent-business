{
  "name": "@personal-agent/installer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vendored, de-tenanted Composio session engine (the minting engine). Runs on the operator's OWN box. ZERO rooster values; secrets via env only.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "receiver": "tsx receiver/server.ts",
    "reconcile": "tsx scripts/reconcile-sessions.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@composio/core": "^0.10.0"
  }
}
