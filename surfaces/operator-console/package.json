{
  "name": "personal-agent-operator-console",
  "version": "0.1.0",
  "private": true,
  "description": "Operator console: the 3-tab management dashboard (Dashboard spend/ROI, New agent mint, Fleet) behind one password gate. A thin proxy to the box receiver via MINT_RECEIVER_URL.",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.0"
  }
}
